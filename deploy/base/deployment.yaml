apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-boilerplate-go
  labels:
    app: ms-boilerplate-go
spec:
  replicas: 3
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: ms-boilerplate-go
  template:
    metadata:
      labels:
        app: ms-boilerplate-go
    spec:
      serviceAccountName: ms-boilerplate-go-sa
      terminationGracePeriodSeconds: 20
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: karpenter.sh/nodepool
                    operator: Exists
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - ms-boilerplate-go
              topologyKey: "kubernetes.io/hostname"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: ms-boilerplate-go
      tolerations:
        - key: "karpenter.sh/provisioner-name"
          operator: "Exists"
          effect: "NoSchedule"
      containers:
        - name: ms-boilerplate-go
          image: 650155821999.dkr.ecr.sa-east-1.amazonaws.com/ms-boilerplate-go:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: ms-boilerplate-go-config
            - secretRef:
                name: ms-boilerplate-go-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          volumeMounts:
            - name: data
              mountPath: /app/data
      volumes:
        - name: data
          emptyDir: {}
