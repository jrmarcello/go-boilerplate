apiVersion: v1
kind: Service
metadata:
  name: gopherplate
spec:
  selector:
    app: gopherplate
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
    - name: grpc
      protocol: TCP
      port: 50051
      targetPort: 50051
  type: ClusterIP
